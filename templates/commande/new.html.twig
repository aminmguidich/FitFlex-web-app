{% extends "base_front_hssan.html.twig" %}

{% block title %}New Commande{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Create a New Commande</h1>

        <div class="row justify-content-center">
            <div class="col-md-8">
                {{ include('commande/_form.html.twig', {'button_label': 'Save'}) }}
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_commande_index') }}" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
{% endblock %}
