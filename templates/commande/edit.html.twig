{% extends "base_front_ayari.html.twig" %}

{% block title %}Edit Commande{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mb-4">Edit Commande</h1>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-body">
                        {{ include('commande/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                </div>

                <div class="mt-3">
                    <a href="{{ path('app_commande_index') }}" class="btn btn-secondary">Back to List</a>
                    
                    {# Bouton Delete coloré #}
                    <a href="#" class="btn btn-danger" onclick="document.getElementById('delete-form').submit()">Delete</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
