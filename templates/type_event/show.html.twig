{% extends 'base_front_ranim.html.twig' %}

{% block title %}Type Event{% endblock %}
{% block body%}

    <h1>TypeEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_event.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_event.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_event_index') }}">back to list</a>

    <a href="{{ path('app_type_event_edit', {'id': type_event.id}) }}">edit</a>

    {{ include('type_event/_delete_form.html.twig') }}

{%endblock%}