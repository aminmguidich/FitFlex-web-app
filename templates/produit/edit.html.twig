{% extends 'backend/base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mb-4">Edit Produit</h1>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-body">
                        {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                </div>

                <div class="mt-3 text-center">
                    <a href="{{ path('app_commande_index') }}" class="btn btn-secondary">Back to List</a>

                    {# Bouton Delete coloré #}
                    <a href="#" class="btn btn-danger" onclick="document.getElementById('delete-form').submit()">Delete</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
